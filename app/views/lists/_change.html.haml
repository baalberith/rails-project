(
= select_tag :list_id, options_for_select( List.find_all_by_user_id(current_user).collect { |list| [list.name, list.id] }, current_list_id ), 
  :onchange => "$.post('#{change_user_lists_path(current_user)}', {'list_id':this.value} );"
)
