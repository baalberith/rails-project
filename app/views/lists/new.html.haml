%h2 New List

= form_for [@user, @list] do |f|
  = render 'shared/errors', :target => @list

  %p
    = f.label :name
    %br
    = f.text_field :name

  %p
    = f.label :description
    %br
    = f.text_area :description

  %p= f.submit
