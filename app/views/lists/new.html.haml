%h2 New List

= form_for @list do |f|
  %p
    = f.label :name
    %br
    = f.text_field :name

  %p
    = f.label :description
    %br
    = f.text_area :description

  = f.hidden_field :user_id, :value => current_user.id

  %p= f.submit